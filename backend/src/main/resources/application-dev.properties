# Local development overrides.
#
# Keep the default application.properties rate limits enabled so production and
# non-dev deployments remain protected. The local backend/.env activates this
# profile during development.
cost-controls.enabled=false
